import styled from 'styled-components';

const FilterHeader = styled.span`
  font-size: 24px;
  line-height: 1.5;
  color: red;
  margin-right: 20px;
`;

const FilterLabel = styled.label`
  padding-left: 20px;
`;

export { FilterHeader, FilterLabel };
